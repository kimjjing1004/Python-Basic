print(abs(-5)) # 절대값
print(pow(4, 2)) # 4^2 = 4*4
print(max(5, 12)) # 둘 중에 제일 큰 값
print(min(5, 12)) # 둘 중에 제일 작은 값
print(round(3.14)) # 반올림 : 3
print(round(4.99)) # 반올림 : 5

from math import * # math 라이브러리에 모든 것을 import 하겠다는 의미
print(floor(4.99)) # 내림. 4 (소수점 뒤를 다 내려버림)
print(ceil(3.14)) # 올림. 4 (소수점 뒤를 다 올려버림
print(sqrt(16)) # 16은 4의 제곱근